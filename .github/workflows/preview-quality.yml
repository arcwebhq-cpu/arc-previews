name: ARC preview quality

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  preview-quality:
    name: ARC preview quality/preview-quality
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      pages: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Require GitHub Actions-only Pages publication
        run: node scripts/verify_pages_source.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: arcwebhq-cpu/arc-site
          ref: main
          path: .arc-site-contract
          persist-credentials: false
      - name: Verify digest-bound ARC site partner contract
        run: node scripts/verify_arc_site_partner_contract.mjs
        env:
          ARC_SITE_DIR: .arc-site-contract
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Install verified ARC site contract dependencies without lifecycle scripts
        run: npm ci --ignore-scripts --no-audit --no-fund --prefix .arc-site-contract
      - name: Reverify ARC site partner contract after dependency installation
        run: node scripts/verify_arc_site_partner_contract.mjs
        env:
          ARC_SITE_DIR: .arc-site-contract
      - run: npx --no-install playwright install --with-deps chromium
      - run: npm test
        env:
          ARC_QA_REAL_IMAGES: "1"
          ARC_QA_PLAYWRIGHT_BROWSER: "1"
          ARC_SITE_DIR: .arc-site-contract
      - name: Verify generated artifacts are committed
        run: test -z "$(git status --porcelain --untracked-files=all -- . ':(exclude).arc-site-contract/**')"
      - if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: preview-quality-failures
          path: test-results/
          if-no-files-found: ignore

  deploy-pages:
    name: Deploy public preview allowlist
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: preview-quality
    runs-on: ubuntu-latest
    timeout-minutes: 10
    concurrency:
      group: arc-previews-pages
      cancel-in-progress: false
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - name: Recheck GitHub Actions-only Pages publication
        run: node scripts/verify_pages_source.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build strict Pages allowlist
        run: npm run build:pages
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: .pages-dist
          include-hidden-files: true
      - name: Deploy public preview allowlist
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
